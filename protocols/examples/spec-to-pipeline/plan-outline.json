{
  "protocol_version": "1.0",
  "milestones": [
    "Implement invite creation and duplicate-pending validation in the backend.",
    "Update the members page so successful invites appear immediately as pending."
  ],
  "dependencies": {
    "Update the members page so successful invites appear immediately as pending.": [
      "Implement invite creation and duplicate-pending validation in the backend."
    ]
  },
  "deliverables": [
    "Invite creation behavior aligned to `sc-send-invite` and `ac-invite-created`.",
    "Pending invite visibility aligned to `ac-pending-visible`.",
    "Duplicate invite blocking aligned to `sc-block-duplicate-invite` and `ac-no-duplicate-pending`.",
    "Focused verification is kept with the product behavior it exercises; workflow-suggested coverage does not become a separate deliverable."
  ]
}
