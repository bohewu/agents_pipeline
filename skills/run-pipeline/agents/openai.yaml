interface:
  display_name: "Run Pipeline"
  short_description: "Run the full agents_pipeline workflow"
  default_prompt: "Use $run-pipeline to execute this high-risk change with full planning, review, and retry gates."
policy:
  allow_implicit_invocation: false
