interface:
  display_name: "Run UX"
  short_description: "Run a profile-aware product UX audit"
  default_prompt: "Use $run-ux to audit this product experience."
policy:
  allow_implicit_invocation: false
